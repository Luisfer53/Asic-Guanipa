'use strict';
/**
 * Seeder legacy desactivado — sustituido por 20260721000001-updated-admin-seed.js
 * que inserta el usuario administrador de forma compatible con el nuevo esquema.
 */
module.exports = {
    async up(queryInterface, Sequelize) {
        return Promise.resolve();
    },
    async down(queryInterface, Sequelize) {
        return Promise.resolve();
    }
};
